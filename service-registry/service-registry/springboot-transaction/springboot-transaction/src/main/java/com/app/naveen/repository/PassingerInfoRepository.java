package com.app.naveen.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.app.naveen.entity.PassingerInfo;

public interface PassingerInfoRepository extends JpaRepository<PassingerInfo, Long>{

}
